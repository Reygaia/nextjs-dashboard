import { Inter } from 'next/font/google';
import { Playwrite_US_Modern } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const playwrite = Playwrite_US_Modern({ style:"normal" });